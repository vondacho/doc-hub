apiVersion: v2
name: doc-em
description: Example mapping board — one story, its rules, their examples and the open questions, read from and written to a .examplemap file
type: application

# Chart version. Bump on every change to the chart itself.
version: 0.1.0

# Version of doc-em shipped by default. Matches "version" in
# doc-em/package.json and is used as the default image tag.
appVersion: "0.0.1"

keywords:
  - astro
  - example-mapping
  - bdd
  - living-documentation
home: https://github.com/vondacho/doc-hub
sources:
  - https://github.com/vondacho/doc-hub/tree/main/doc-em
maintainers:
  - name: Olivier von Dach
