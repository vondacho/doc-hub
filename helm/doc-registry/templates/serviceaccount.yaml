{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "doc-registry.serviceAccountName" . }}
  labels:
    {{- include "doc-registry.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The CMS does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
