apiVersion: v1
kind: Service
metadata:
  name: {{ include "doc-sm.fullname" . }}
  labels:
    {{- include "doc-sm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doc-sm.selectorLabels" . | nindent 4 }}
