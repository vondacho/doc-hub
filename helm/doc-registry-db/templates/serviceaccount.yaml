{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "doc-registry-db.serviceAccountName" . }}
  labels:
    {{- include "doc-registry-db.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The database does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
