apiVersion: v1
kind: Service
metadata:
  name: {{ include "doc-registry-db.fullname" . }}
  labels:
    {{- include "doc-registry-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "doc-registry-db.selectorLabels" . | nindent 4 }}
