apiVersion: v1
kind: Service
metadata:
  name: {{ include "doc-portal.fullname" . }}
  labels:
    {{- include "doc-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doc-portal.selectorLabels" . | nindent 4 }}
